## ISSUE 一覧

### #73 [Refactor] CODE_REVIEWの現状設計に基づいてドキュメントの修正と整理

**テンプレート: refactoring**

## 対象

`docs/`内の全ドキュメントファイルから必要に応じて

## 現状の問題

初期設計から現状の実装はかなり変更があるが、`docs/`の設計周りのファイルでは前の設計が残っているためAI駆動開発においてAIが間違った情報を参照してしまう。

## 目的

`docs/CODE_REVIEW.md`に記述されている現状の設計を正として`docs/`周りのドキュメントを修正。それに合わせて冗長な部分や読みにくい部分があれば整理する。

## 非目的

実装コード側（`src/`など）は絶対に編集・参照しない。

## 実施内容

- [ ] `docs/`内のドキュメントをCODE_REVIEW.mdの設計に基づいて修正・整理

## 受け入れ条件

- [ ] `docs`内のドキュメントが正の情報に基づいて記述されている

## リスク・注意点

`src/`ファイルおよび実装コードはいっさい変更あるいは修正しない。

### #74 [Refactor] CODE_REVIEWの現状設計に基づいて実装コードの修正・整理

**テンプレート: refactoring**

## 対象

`src/`を含む全ての実装コードから必要に応じて

## 現状の問題

初期設計から現状の実装はかなり変更があるが、`src/`を含む全ての実装コードでは前の設計が残っているため使用していないあるいは必要のないコードが残っている。

## 目的

`docs/CODE_REVIEW.md`に記述されている現状の設計を正として`src/`を含む全ての実装コードを必要であれば修正しリファクタリングする。

## 非目的

`docs/`内のドキュメント側は絶対に編集・参照しない。

## 実施内容

- [ ] `src/`を含む全ての実装コードをCODE_REVIEW.mdの設計に基づいて修正・リファクタリング

## 受け入れ条件

- [ ] `src/`を含む全ての実装コードが正の情報に基づいて修正・リファクタリングされている。

## リスク・注意点

`docs/`内のドキュメント側はいっさい変更あるいは修正しない。

### #75 [Bug] 環境変数の未検証・型不安全な参照

**テンプレート: bug_report**

## 現象

`src/lib/` 配下の複数ファイルで `process.env.XXX` を `!`（Non-null assertion）や型キャストなしで直接参照している箇所がある。未設定の環境変数にアクセスした場合、デプロイ後に実行時エラーが発生する。

## 期待結果

必須環境変数が未設定の場合、アプリ起動時に明確なエラーメッセージを出してフェイルファストで停止する。

## 再現手順

1. `DATABASE_URL` や `AUTH_SECRET` を `.env` から削除する
2. `npm run preview` を実行する
3. リクエストを送ると実行時エラーが発生する（起動時ではなくリクエスト時に気づく）

## 環境

- ランタイム: Cloudflare Workers（`@opennextjs/cloudflare` v1）
- 対象ファイル: `src/lib/prisma.ts`, `src/lib/stripe.ts`, `src/lib/mailer/` 配下など

## 補足

対策案: `src/lib/env.ts` を作成し Zod で起動時バリデーションを行う（⑥-1 参照）。優先度: 🔴 高

### #76 [Bug] Cron notify エンドポイントの部分失敗時エラーハンドリング不足

**テンプレート: bug_report**

## 現象

`/api/cron/notify` の `findAndDeliverNotifications` が複数ユーザーへのメール送信を行う際、一部が失敗しても処理全体が `500` を返す可能性がある。成功分と失敗分が混在するケースへの対応が曖昧。

## 期待結果

部分失敗時は成功件数・失敗件数をレスポンスに含め、`200` または `207` を返す。失敗分は DB に `status = 'failed'` として記録する。

## 再現手順

1. Resend API キーを無効にした状態でCronを発火する
2. 一部のメール送信が失敗する
3. 全体のステータスコードと DB の記録状態を確認する

## 環境

- ランタイム: Cloudflare Workers
- 対象ファイル: `src/app/api/cron/notify/route.ts`, `src/lib/notifications/notify.ts`

## 補足

Cron の冪等性は確保済み。優先度: 🔴 高

### #77 [Bug] Magic Link トークンの期限切れと改ざんで同一エラー応答

**テンプレート: bug_report**

## 現象

`/api/auth/verify` でトークン検証に失敗した場合、「期限切れ」と「改ざん/不正」のどちらも同じ `?error=expired` にリダイレクトされる可能性がある。ユーザーに適切なメッセージが伝わらない。

## 期待結果

- 期限切れ → `?error=expired`（「リンクが期限切れです。再送してください」）
- 不正トークン → `?error=invalid`（「無効なリンクです」）

## 再現手順

1. 期限切れトークンでアクセスする → エラー種別を確認
2. 存在しないトークン文字列でアクセスする → エラー種別を確認

## 環境

- 対象ファイル: `src/app/api/auth/verify/route.ts`, `src/lib/auth/token.ts`

## 補足

優先度: 🟡 中

### #78 [MVP] デプロイ構成の一本化（Cloudflare Workers 正式採用の明文化）

**テンプレート: mvp_task**

## 目的

`vercel.json`（Cron 設定のみ残存）と Cloudflare Workers 設定が混在しており、どちらが「正」の運用かが不明確な状態を解消する。

## 背景

現状 Cloudflare Workers（`wrangler.toml` + `open-next.config.ts`）が本番デプロイの正として運用されているが、`vercel.json` が残っており混乱の原因になりうる。コードレビュー 2-3・4-1 に対応。

## スコープ

- ## やること: `vercel.json` の用途をコメントで明示するか削除する。`README.md` にデプロイ手順を明記する。
- ## やらないこと（非目的）: Vercel 対応コードの追加

## 受け入れ条件（AC）

- [ ] `vercel.json` の扱い（削除 or コメント追記）が決定・実施されている
- [ ] `README.md` に「デプロイ = Cloudflare Workers」と明記されている
- [ ] `wrangler.toml` のシークレット登録手順が README または RUNBOOK に記載されている

## 優先度

- P1（MVP必須）

## 推定

- S（1〜3h）

## 手動確認

- `npm run deploy` が正常に完了すること

### #79 [MVP] `docker-compose.yml` の用途明確化または削除

**テンプレート: mvp_task**

## 目的

`docker-compose.yml` が実際にローカル開発で使われているか確認し、不要であれば削除してリポジトリをクリーンに保つ。

## 背景

DB は Neon（クラウド）を使用しており、ローカル PostgreSQL が不要な可能性がある。コードレビュー 4-2 に対応。

## スコープ

- ## やること: `docker-compose.yml` の用途確認・削除または README への起動手順追記
- ## やらないこと（非目的）: ローカル PostgreSQL 環境の新規構築

## 受け入れ条件（AC）

- [ ] `docker-compose.yml` を削除、または `README.md` に使用用途と起動手順が記載されている

## 優先度

- P2（できれば）

## 推定

- S（1〜3h）

## 手動確認

- 削除した場合、既存の開発フローに影響がないこと

### #80 [Feature] 環境変数の一元バリデーション実装

**テンプレート: feature_request**

## 背景

現状、必須環境変数が未設定でも起動時にエラーにならず、リクエスト処理中に実行時エラーが発生する（#75 と関連）。

## 目的

`src/lib/env.ts` を作成し、アプリ起動時に全必須環境変数を Zod で検証してフェイルファストにする。

## 非目的

Zod 以外のバリデーションライブラリへの移行は今回対象外。

## 受け入れ条件

- [ ] `src/lib/env.ts` が存在し、必須環境変数を `z.object({...}).parse(process.env)` で検証している
- [ ] 未設定の必須変数がある場合、起動時に明確なエラーメッセージが表示される
- [ ] 全 `src/lib/` ファイルが `process.env.XXX` 直接参照から `env.XXX` 参照に差し替えられている

## 代替案

- `@t3-oss/env-nextjs` などの専用ライブラリを使う（依存追加になるため今回は見送り）

### #81 [Feature] Magic Link 送信エンドポイントへのレート制限追加

**テンプレート: feature_request**

## 背景

`/api/auth/magic-link`（Magic Link 送信）にレート制限がなく、同一 IP またはメールアドレスへの大量リクエストによるメール爆弾攻撃が可能な状態。

## 目的

Magic Link 送信 API に IP ベースまたはメールアドレスベースのレート制限を追加し、悪用リスクを軽減する。

## 非目的

全 API エンドポイントへのレート制限適用（今回は Magic Link 送信のみ）。

## 受け入れ条件

- [ ] 同一 IP から短時間に大量リクエストした場合 `429 Too Many Requests` が返る
- [ ] レート制限のしきい値が設定可能（環境変数 or 定数）

## 代替案

- Cloudflare Workers の組み込みレート制限機能を使う
- Upstash Redis + `@upstash/ratelimit` を使う（推奨）

### #82 [Feature] プラン定義定数の分離（`FREE_FEATURES` / `PRO_FEATURES`）

**テンプレート: feature_request**

## 背景

`src/app/page.tsx` に `FREE_FEATURES`・`PRO_FEATURES` などのプラン定義定数がコンポーネント内にハードコードされており、プラン仕様変更時に UI 側のみ修正が必要になる。

## 目的

プラン定義を `src/config/plans.ts` などの設定ファイルに分離し、UI・ロジック双方から参照できるようにする。

## 非目的

プラン構成自体の変更。

## 受け入れ条件

- [ ] `src/config/plans.ts`（または同等のファイル）にプラン定義が集約されている
- [ ] `src/app/page.tsx` がそのファイルを参照している
- [ ] `src/lib/deadlines/gate.ts` など他の箇所でも同定義を参照できる構造になっている

## 代替案

なし

### #83 [Feature] テストランナーの Vitest 移行

**テンプレート: feature_request**

## 背景

現状のテストは `node:assert` + `npx tsx` による手動実行方式で、カバレッジ計測・CI 統合ができない。

## 目的

Vitest を導入し、`npm test` 一コマンドで全テストを実行・カバレッジを計測できるようにする。

## 非目的

E2E テスト（Playwright 等）の導入は今回対象外。

## 受け入れ条件

- [ ] `vitest` が devDependencies に追加されている
- [ ] 既存の `__tests__/` 配下のテストが Vitest で実行できる
- [ ] `npm test` でテストが実行される
- [ ] CI（GitHub Actions）でテストが自動実行される

## 代替案

Jest（設定が複雑になりやすいため Vitest を推奨）

### #84 [Feature] `src/` ディレクトリ構造の `features/` 分割リファクタリング

**テンプレート: feature_request**

## 背景

現状 `src/lib/` にビジネスロジックが平置きされており、ドメインの境界が不明確。スケール時に見通しが悪くなる。

## 目的

`src/features/` ディレクトリを導入し、`deadlines` / `auth` / `billing` / `notifications` ドメインごとにファイルを分割する。

## 非目的

`src/app/` 配下の API ルート・ページ構成の変更（現状維持）。

## 受け入れ条件

- [ ] `src/features/{domain}/` 構造が作成されている
- [ ] `src/lib/` には汎用ユーティリティ（DB接続・メール基盤など）のみ残っている
- [ ] 既存のインポートパスが更新されビルドが通っている

## 代替案

現状維持（MVP 段階では許容範囲）
